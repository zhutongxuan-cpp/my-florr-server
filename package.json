{
  "name": "my-florr",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  // 👈 Render 启动时会找这行命令
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}